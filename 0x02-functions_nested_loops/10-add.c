#include "main.h"

/**
 * add - Adds two integers and returns
 * the result.
 *
 * @e: number one.
 * @f: number two.
 *
 * Return: Add of number one and number two.
 */
int add(int e, int f)
{
	return (e + f);
}
